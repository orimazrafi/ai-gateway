# Optional: upstream and API key
# OPENAI_API_KEY=sk-...
# AI_GATEWAY_UPSTREAM=https://api.groq.com/openai/v1

# Optional: SQLite database (user settings, costs, prompt log persist across restarts)
# DATABASE_PATH=./data/gateway.sqlite
# Requires: npm install better-sqlite3 (optional dependency)

# SSO (Google OAuth)
# GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=xxx
# JWT_SECRET=your-secret-for-signing-session-tokens
# DASHBOARD_URL=http://localhost:5173
# GATEWAY_PUBLIC_URL=http://localhost:3002
