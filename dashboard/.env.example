# Gateway URL for Next.js rewrites (server-side proxy). Required on Vercel.
# Do NOT set NEXT_PUBLIC_GATEWAY_URL — the dashboard uses relative URLs and rewrites.
GATEWAY_URL=https://ai-gateway.up.railway.app
